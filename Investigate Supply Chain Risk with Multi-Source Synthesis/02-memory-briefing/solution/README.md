# Exercise 2 (Solution): From Claims to an Honest Briefing

The finished shared memory and synthesis briefing. Compare against your work.

```bash
python3 -m venv .venv && .venv/bin/pip install -e ".[dev]"
.venv/bin/pytest tests/test_memory.py tests/test_synthesis.py -q
```
