# Exercise 1 (Solution): One Claim Shape for Every Source

The finished `Claim` model and the four readers. Compare against your work.

```bash
python3 -m venv .venv && .venv/bin/pip install -e ".[dev]"
.venv/bin/pytest tests/test_readers.py -q
```
